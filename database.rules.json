{
  "rules": {
    "$location": {
      "$date": {
        "volunteers": {
          "$uid": {
            ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
          }
        }
      }
    }
  }
}
